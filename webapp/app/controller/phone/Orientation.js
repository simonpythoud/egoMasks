Ext.define('EgoMasks.controller.phone.Orientation', {
    extend: 'EgoMasks.controller.Orientation',
    
    config: {
        refs: {
            overview: '#overview'
        }
    },

    launch: function(app) {
        this.callParent();
    }
});